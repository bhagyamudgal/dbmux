# Changelog

`dbmux` is the only published package in this repository, so its changelog
lives with it: [packages/cli/CHANGELOG.md](packages/cli/CHANGELOG.md).

Entries are generated from changesets — see [.changeset/README.md](.changeset/README.md)
to add one.
